services:
  - type: web
    name: nai-dang-services
    runtime: node
    buildCommand: npm install
    startCommand: node web-server.mjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3002
